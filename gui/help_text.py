"""使用说明的文字内容（纯 HTML，给 QTextBrowser 显示）。

为什么单独放一个文件：说明文字会经常改，改这个文件不用动 main.py；
而且它就是一个普通模块，打包时会被自动收进去，不用额外拷资源文件。

写作角度：全部按"使用者会遇到什么"来组织，不写代码细节。
"""
HELP_HTML = """<html><body style="font-size:13px; line-height:150%;">

<h2>使用说明 · 从第一次打开到看懂每一条提示</h2>
<p>这个软件就四件事：<b>抓网页</b>、<b>采固定站点</b>、<b>把本地文档做文本分析</b>、<b>把数据画出来</b>。
上面两行按钮负责"做事"，下面几个标签页负责"反馈结果"。下面按你可能遇到的问题来写。</p>
<hr>

<h3>一、三分钟上手</h3>
<ul>
<li><b>抓一个网页</b>：在 URL 框里输入网址 → 点「开始抓取」（按回车也行）。
写不写 <code>https://</code> 都可以，程序会自动补上。勾选「抓站内子页」并设置「上限」，
就会顺着站内链接继续抓，最多抓那么多页。
结果落在 <code>data\\&lt;域名&gt;\\&lt;日期_时间&gt;\\pages.csv</code>，去「资源管理」页就能看到。</li>
<li><b>采 5 个固定站点</b>：点「开始采集（run_daily）」，依次采百度、哔哩哔哩、豆瓣、掘金、头条，
日志会实时滚动。采完会<b>自动接着生成报告</b>，不用你再点一次。</li>
<li><b>出报告</b>：点「生成报告（analysis.report）」，结果放在 <code>reports\\&lt;日期_时间&gt;\\</code>：
十几张 CSV + <code>看板.html</code> + 离线用的 <code>assets\\echarts.min.js</code>。
跑完会自动刷新并切到「筛选看板」页。</li>
<li><b>分析本地文档</b>：切到「文本分析」页 → 「选择目录…」选一个放文档的文件夹 → 点「① 开始抽取」（抽完会<b>自动出图表</b>）。支持 docx / txt / md / csv / 文字型 pdf。</li>
<li><b>看归档</b>：每一批报告目录里仍然有一份 <code>看板.html</code>，用浏览器双击打开就能看（离线也行）。</li>
<li><b>看数据</b>：「资源管理」页左边是 <code>data\\</code> 文件树，点 CSV 右边显示表格（前 200 行），
点其它文本文件显示前 100 行。</li>
</ul>

<h3>二、你可能会发现的问题（以及答案）</h3>

<h4>窗口与托盘</h4>
<ul>
<li><b>问：我点了右上角的 ×，窗口不见了，程序关了吗？</b><br>
没关，只是收进托盘了（右下角那个蓝底「爬」字的图标）。第一次会弹一句气泡提醒，
标题就是「还在后台运行」。这么做是因为「开机自启」是静默启动的，
如果点 × 就直接退出，你会以为程序坏了或者被杀掉了。<br>
真要退出：<b>右键托盘图标 → 退出程序</b>。如果这时候还在抓取/采集，它会先问你一次再退。</li>
<li><b>问：托盘图标在哪？长什么样？</b><br>
蓝底白「爬」字。右下角图标太多时，点那个向上的小箭头 <code>^</code> 展开就能看到它。</li>
<li><b>问：点 × 不退出，会不会在后台一直吃资源？</b><br>
空闲时几乎不耗（没有轮询、没有定时循环）；只有你点按钮跑任务时它才活动。</li>
</ul>

<h4>按钮与任务</h4>
<ul>
<li><b>问：点「开始采集」或「生成报告」提示「上一次任务还在运行」？</b><br>
采集和生成报告不能同时跑（它们都要读写 <code>data\\</code> 和 <code>reports\\</code>），
所以上一个还没结束就会被拒绝。等它跑完——日志里会出现「[进程结束] 退出码 = 0」——
按钮会自己恢复可点。</li>
</ul>

<h4>抓取相关</h4>
<ul>
<li><b>问：输入网址后提示"本次爬取已取消"，为什么？</b><br>
这是<b>域名校验</b>没过。会被拦的情况：空的、带空格的、<code>ftp://</code> 或 <code>file://</code>
这类非 http/https、主机名里没有点（<code>百度</code>、<code>localhost</code>）、
后缀不对（<code>abc.123</code>）、端口写错（<code>:abc</code>）、括号不配对（<code>https://[abc/</code>）。
先拦下来是为了不让你白等——否则程序会拿这个假地址重试 4 次、每次隔 1~5 秒。</li>
<li><b>问：点「开始抓取」提示「上一次抓取还在进行」？</b><br>
抓取是"一次只跑一个"：上一次还没结束就又点，程序会直接拒绝，避免两次抓取撞进同一个 CSV。
等它跑完再点；如果日志很久没动，多半是卡在重试上（每次重试要等 1~5 秒，最多 4 次）。</li>
<li><b>问：有些网址抓不了，日志说 robots 不允许？</b><br>
这是合规设计：抓之前先请求该站的 <code>robots.txt</code>，它说不让抓就不抓。
例如 <code>api.bilibili.com</code> 写的是 <code>Disallow: /</code>，所以 B 站榜单改从
<code>tophub.today</code> 取；每个快照里都有 <code>via</code> 列记着数据到底来自哪儿。
robots.txt 不存在（404）按"允许"处理。</li>
<li><b>问：抓取为什么这么慢？</b><br>
每个请求之间随机停 1~5 秒（礼貌爬取：既不给对方压力，也降低被封的概率）；
遇到 429 限流会退避后重试，最多 4 次。</li>
<li><b>问：只抓了两三页就停了？</b><br>
先看「上限」那个数字（默认 20 页）。另外站内链接会过滤掉图片、样式、文档、压缩包等非网页链接，
站外链接也不抓。</li>
<li><b>问：CSV 里的 <code>content</code> 列特别长，是什么？</b><br>
是页面正文的纯文本（已去掉 script/style/iframe 等标签），一行一页，方便以后做分词或检索。</li>
</ul>

<h4>数据、文件与启动提示</h4>
<ul>
<li><b>问：启动时那句「[清理] 删掉 N 个空目录」，会不会把我数据删了？</b><br>
不会。只删<b>整棵子树里一个文件都没有</b>的目录，而且<b>10 分钟内新建的目录一律不碰</b>
（怕删掉正在进行的抓取刚建出来的目录）。有文件的目录、<code>reports\\</code>、<code>logs\\</code>
都不在清理范围内。空目录是抓取失败（robots 拒绝、网络不通）时留下的残渣。</li>
<li><b>问：「[清理] 删掉过期的采集锁」是什么？</b><br>
采集前会在 <code>logs\\runner.lock</code> 放一把"锁"，防止两次采集撞在一起。
正常结束会自己删掉；如果是被强制关闭留下的，它会挡住后面的采集，
所以<b>超过 30 分钟</b>就当作"上次异常退出"，在启动时清掉。</li>
<li><b>问：日志说"上一次采集还在进行（xx 秒前开始），本次跳过"？</b><br>
就是那把锁还在（30 分钟以内）。等一会儿再点；确认没有别的采集在跑，
也可以直接删掉 <code>logs\\runner.lock</code>。</li>
<li><b>问：「数据概况」那 5 行是什么？数字越大越糟吗？</b><br>
那是仓库体检，比的是"磁盘上有什么"和"登记表里记了什么"：
<ul>
<li><b>未登记</b>：磁盘上有这个 CSV，但登记表里没有 → 多半是没用的残留</li>
<li><b>登记了但磁盘没有</b>：登记了却没文件 → 该重新采了</li>
<li><b>空快照</b>：目录建了但没有 CSV → 抓取失败留下的</li>
<li><b>含其他数据集</b>：这个快照目录里混了别的 CSV</li>
<li><b>老格式快照</b>：缺 <code>source</code> 列的旧格式快照</li>
</ul>
前两类正常应该是 0（或者你能解释清楚）；后三类属于历史遗留，不影响看板和分析。</li>
<li><b>问：有的格子是空的，或者排名显示成 1.0？</b><br>
各站点字段本来就不一样：豆瓣有评分没有热度值，百度榜单没有热度值，
掘金的 <code>score</code> 其实是它的 hot_rank，头条的 <code>HotValue</code> 才是排序键。
空值属于正常。整数列已经转成 Int64，不会再出现 <code>3340464.0</code> 这种写法。</li>
<li><b>问：<code>data\\</code> 里为什么有看着像残缺域名的文件夹（比如 <code>demo-</code>）？</b><br>
一个域名一个目录，名字直接取自当时输入网址的主机名。输入残缺时目录名就是残缺的；
如果里面是空的，下次启动会被自动清掉。</li>
</ul>

<h4>报告与看板</h4>
<ul>
<li><b>问：「筛选看板」页空白、图表不出来？</b><br>
先点上面的<b>「生成视图」</b> —— 它要跑几秒钟（按你选的条件重算十几张表，界面不会卡）。
算完会自动切到这一页，里面有三个子页：<b>总览看板</b>（所有图）、
<b>单源指标</b>（选一张表：看图 + 看数据 + 另存成 CSV/Excel）、
<b>跨源对比</b>（各源翻新速度、同话题名次对比）。</li>
<li><b>问：为什么有的表点开没有图？</b><br>
像「跨源_同日Top5 / 同日TopN」这种是<b>明细列表</b>——一行就是"哪天哪个源第几名是什么"，
画成图只会一团乱，所以它们只配数据表，那一页会提示你直接看下面的表。</li>
<li><b>问：报告里为什么有十几张 CSV？</b><br>
分两类：单源表（豆瓣评分分布/年代分布/票数增长、百度标签与分类、B 站评分与播放增长、
掘金作者榜、头条标签热榜…）和跨源表（覆盖情况、同日 Top5、同话题、翻新速度）。
「筛选看板」页是把主要结论画成图。</li>
<li><b>问：跨源分析为什么只比排名，不比热度？</b><br>
因为各站热度口径完全不同（豆瓣是评分、头条是 HotValue、掘金是 hot_rank、百度没有），
拿来比就是错的。只有"排名"是各站共同且同质的字段，所以跨源只比 rank。</li>
</ul>

<h4>文本分析（本地文档）</h4>
<ul>
<li><b>问：这个功能是干什么的？</b><br>
把你电脑上的文档拿来做统计：<b>docx / txt / md / csv / 文字型 PDF</b>。
先"抽取"（把文件变成一条条文本），再算六个指标：总文本条数、总字数、总词数、
去重词汇量、平均句子长度、缺失文本数量，最后出图。</li>
<li><b>问：怎么用？</b><br>
切到「文本分析」页 → 「选择目录…」选一个放文档的文件夹 → 点「① 开始抽取」，
<b>抽完会自动接着出图表</b>（不用再点一次）。两个子页看结果：「全语料总览」和「分文档图」。
想知道文件在哪，点「打开报告目录」。</li>
<li><b>问：每份文档的 5 张图是什么？</b><br>
① 词频占比（饼图，最多 8 片，其余合并成「其他」）② 词频 Top20（排行）
③ 句子长度分布 ④ 平均句子长度（这份文件 vs 全语料，两根柱）
⑤ 词数 vs 去重词汇量（并排两根柱：去重占比越低，说明用词越重复）。</li>
<li><b>问：为什么扫描件 PDF 抽不出文字？</b><br>
只做<b>文字型</b> PDF（能用鼠标选中文字的）。扫描件是图片，要 OCR，本软件不做 ——
这类文件会被算进「缺失文本」并写明原因（缺失数字会标红提醒你）。</li>
<li><b>问：.doc（老版 Word）为什么不支持？</b><br>
老格式是二进制，python-docx 读不了。用 Word 打开后「另存为 .docx」再放进来就行。
CSV 里如果没有"正文/内容"这种列，程序会按整行拼接，并把这个事实写进说明。</li>
<li><b>问：为什么"总字数"和 Word 里统计的不一样？</b><br>
本软件的口径是<b>去掉空白字符后的字符数（含标点）</b>，Word 是另一套口径。
所有口径都写在报告目录的 <code>_说明.txt</code> 里，写论文可以直接引用。</li>
<li><b>问：打包版看到的报告和开发时不一样？</b><br>
打包版的 data 目录和 reports 目录在 <b>exe 旁边</b>，和项目根目录的不是同一份。
语料目录是你自己选的，放哪都行。</li>
</ul>

<h4>定时采集与开机自启（需要先打包成 exe）</h4>
<ul>
<li><b>问：怎么让数据自己长起来？</b><br>
打包出 exe 之后，双击 <code>tools\\install_task.bat</code> 安装定时任务：
每天 <b>10:00 / 15:00 / 19:00</b> 自动"采集 5 个站点 + 生成报告"。
装完可以双击 <code>tools\\run_task_now.bat</code> 立刻试跑一次，
<code>tools\\task_status.bat</code> 看结果（结果码 0 = 成功，2 = 找不到程序）。</li>
<li><b>问：到点了却没跑？</b><br>
这个任务默认"只在你登录时运行"。如果那个时间电脑在睡眠/关机，Windows 会在下次可用时补跑
（已经设置了"错过就尽快补跑"）。也可能是 exe 被挪走了——重装一次任务即可。</li>
<li><b>问：怎么关掉定时任务 / 开机自启？</b><br>
<code>tools\\remove_task.bat</code> 和 <code>tools\\remove_autostart.bat</code>。
开机自启也可以直接在「任务管理器 → 启动」里禁用。</li>
<li><b>问：装了开机自启，开机后怎么没看到窗口？</b><br>
这是故意的：自启会带 <code>--minimized</code>，启动后直接进托盘，不弹窗打扰你。
想看界面就点一下托盘图标（或双击）。</li>
</ul>

<h4>安全软件与其它</h4>
<ul>
<li><b>问：杀毒软件/安全卫士报警、甚至直接把 exe 删了？</b><br>
自研的小程序没有代码签名，加上 PyInstaller 打包的 exe 本来就是误报重灾区
（很多人拿它做坏事，杀软就按"壳"的特征一刀切）。处理办法：
把软件文件夹加入白名单/信任区，或提交误报申诉。<br>
注意：<b>不要为了过杀软去加壳、混淆、加密</b>——那会让它更像病毒，也涉及学术诚信。</li>
<li><b>问：换台电脑、或者要在老师电脑上演示？</b><br>
把整个软件文件夹一起拷走（exe + <code>_internal</code> + <code>data</code> + <code>reports</code>），
数据跟着走，属于绿色免安装。第一次在别人电脑上跑，记得先加白名单。</li>
<li><b>问：数据能直接改吗？</b><br>
<code>data\\</code> 是原始快照，最好别手动改（它是"证据"）；
<code>reports\\</code> 是产物，删掉重新生成就行。</li>
</ul>

<h3>三、真出问题了，怎么自己先查一遍</h3>
<ol>
<li>看「运行日志」最后几行，或 <code>logs\\</code> 里当天的日志文件（中文乱码说明编码设置被改过）。</li>
<li>三条自检命令（在项目根目录执行）：
<ul>
<li><code>python -m utils.housekeeping</code> —— 手动清一次空目录和过期锁</li>
<li><code>python -m warehouse.boards</code> —— 打印仓库总行数、各数据集行数、体检结果</li>
<li><code>python -m analysis.common</code> —— 打印各数据集"全部/最新快照"行数与频次表自检</li>
</ul></li>
<li>报告打不开时，看 <code>reports\\&lt;最新一批&gt;\\_说明.txt</code>（里面记着这批报告的时间和数据量）。</li>
<li>还不行：把「运行日志」的内容 + 你点过哪些按钮、按什么顺序点的，一起发出来。</li>
</ol>

</body></html>"""
